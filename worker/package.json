{
  "name": "universal-import-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "echo 'no build needed (tsx runs TS directly)' && exit 0",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "fastify": "^5.2.0",
    "lru-cache": "^11.0.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
